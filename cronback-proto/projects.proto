syntax = "proto3";

import "common.proto";

package projects;

enum ProjectStatus {
    ProjectStatus_UNKNOWN = 0;
    ENABLED = 1;
    DISABLED = 2;
    QUOTA_EXCEEDED = 3;
    PENDING_DELETION = 4;
}
